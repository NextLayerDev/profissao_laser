/**
 * MIGRAÇÃO — Onda 1 · módulo `subscriptions`
 * Origem legada: src/types/addons.ts
 *
 * [ ] Mover os tipos de src/types/addons.ts para cá.
 * [ ] Adicionar schema Zod (`addonSchema`) e derivar o type dele quando fizer
 *     sentido (ver modules/courses/types/courses.ts como referência).
 * [ ] Exportar type + schema no index.ts do módulo.
 */

// import { z } from 'zod';
// export const addonSchema = z.object({ ... });
// export type Addon = z.infer<typeof addonSchema>;

export {};
