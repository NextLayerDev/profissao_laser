/**
 * MIGRAÇÃO — Onda 1 · módulo `subscriptions`
 * Origem legada: src/types/entitlements.ts
 *
 * [ ] Mover os tipos de src/types/entitlements.ts para cá.
 * [ ] Adicionar schema Zod (`entitlementSchema`).
 * [ ] Exportar type + schema no index.ts do módulo.
 */

// import { z } from 'zod';
// export const entitlementSchema = z.object({ ... });
// export type Entitlement = z.infer<typeof entitlementSchema>;

export {};
