/**
 * A tela do VÍDEO DO ANÚNCIO (`ui.layout: 'video'`). Barril fino, igual ao do
 * Ateliê: quem despacha o layout (`dynamic-tool-view`) importa daqui e não
 * precisa saber em quantos arquivos a tela está partida.
 */

export type { ToolVideoViewProps } from './tool-video-view';
export { ToolVideoView } from './tool-video-view';
