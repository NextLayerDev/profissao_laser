/**
 * MIGRAÇÃO — Onda 1 · módulo `access`
 * Origem legada: src/types/auth.ts
 *
 * [ ] Mover os tipos de src/types/auth.ts para cá.
 * [ ] Adicionar schema Zod (`loginResponseSchema`, etc.).
 * [ ] Exportar type + schema no index.ts do módulo.
 */

// import { z } from 'zod';
// export const loginResponseSchema = z.object({ ... });
// export type LoginResponse = z.infer<typeof loginResponseSchema>;

export {};
