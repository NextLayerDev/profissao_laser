/**
 * MIGRAÇÃO — Onda 1 · módulo `courses`
 * Origem legada: src/types/classes.ts
 *
 * [ ] Mover os tipos de src/types/classes.ts para cá.
 * [ ] Adicionar schema Zod (`classSchema`).
 * [ ] Exportar type + schema no index.ts do módulo.
 */

// import { z } from 'zod';
// export const classSchema = z.object({ ... });
// export type Class = z.infer<typeof classSchema>;

export {};
